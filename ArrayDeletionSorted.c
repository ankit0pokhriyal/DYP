#include<stdio.h>
int main(){
    int i, n, j, num, arr[20];
    printf("Enter the No. of elements:");
    scanf("%d", &n);
    for(i=0;i<n;i++)
    {
        printf("\n  arr[%d]:", i);
        scanf("%d", &arr[i]);

    }
    printf("Enter the No. to be Deleted:");
    scanf("%d", &num);
    // Write the code for deleting the element and the update the size.
    // CODE HERE 
    
    printf("The array after Deletion is:");
    // Print the Number After Deletion.
    // CODE HERE 
    return 0;
}